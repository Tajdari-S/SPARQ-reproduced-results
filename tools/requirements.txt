# Python dependencies for the figure scripts and analysis tooling.
# Versions are those tested on the measurement host (Python 3.10.12).
numpy==1.26.4
matplotlib==3.8.4
pandas==2.2.2
scipy==1.13.1
# GPU baseline only; install from the RAPIDS channel, not PyPI.
# See SPARQ/baselines/gpu/environment_observed.txt for the exact stack.
#   cudf, dask-cudf, dask-cuda, rmm, cupy
